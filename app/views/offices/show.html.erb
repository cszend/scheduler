<% provide(:title, @office.name) %>
<h1><%= @office.name %></h1>
<!-- Time.zone.now
     Time.zone.today -->
<%= @office.description %>

<p>Providers</p>
<%= render @providers %>

<%= link_to "Add a Staff Member", new_office_provider_path(@office), class: "btn btn-large btn-primary" if has_access_level?(4, @office.id) %>
